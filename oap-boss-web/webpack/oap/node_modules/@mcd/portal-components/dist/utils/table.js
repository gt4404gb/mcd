import { getValueByKey } from "./common";
export function getPageSize() {
  return getValueByKey("portal.pagesizes") || 50;
} // 当列表页面有标签组件这种特殊场景，需要传入otherHeight

export function getTableHeight(pageClassName) {
  var otherHeight = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 187;

  if (!pageClassName) {
    return 0;
  } // 搜索区的高度


  var antForm = document.querySelector(".".concat(pageClassName, " .ant-form")); // 列表上面筛选组件的高度

  var tableTop = document.querySelector(".".concat(pageClassName, " .table-top")); // 列表下面保存取消的高度

  var tableFooter = document.querySelector(".".concat(pageClassName, " .table-footer"));
  var autoHeight = otherHeight;
  antForm && (autoHeight += antForm.clientHeight);
  tableTop && (autoHeight += tableTop.clientHeight);
  tableFooter && (autoHeight += tableFooter.clientHeight);
  return autoHeight;
} // 获取tableKey，防止一个页面多个列表时筛选条件冲突

export function getTableKey() {
  var key = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "table";
  var USER_INFO = JSON.parse(localStorage.getItem("USER_INFO"));
  var pathname = window.location.pathname;
  return pathname + "@" + key + "-" + USER_INFO.id;
}