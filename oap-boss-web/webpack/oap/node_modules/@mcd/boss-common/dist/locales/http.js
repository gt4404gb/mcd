import { post as commonPost, get as commonGet } from '../utils/http';
import { ApiPrefix } from '../utils/ApiConstants';
export function post(url, param) {
  return commonPost(url, param, ApiPrefix.FOUNDATION);
}
export function get(url, param) {
  return commonGet(url, param, ApiPrefix.FOUNDATION);
}